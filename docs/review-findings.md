# Development review

This review covers the current frontend, native worker, page editor, IPC, update dialog and release scripts. It is an incremental review of the source build, not completion of the requested final review or certification of the published installer.

## Fixed with regression coverage

| Finding | Change and evidence |
| --- | --- |
| Final standalone validation for the Viewer scroll update. | The debug/no-bundle build exited 0 with 1,936 frontend modules in 2.65 seconds and native compilation in 21.67 seconds. Its 30,177,792-byte executable SHA-256 is `CA50BB8E215821F61FC6371783D7233C6BCD1E97311DFC8A498C23DF252E164D`; exact `index-CtBH9GLt.js`, CSS, and transformed HTML Brotli bytes were verified embedded. This validates the packaged source build, not native rendering behavior or a large-viewer performance benchmark. |
| Viewer scroll could scan every page to find the visible range and current physical page. | Viewer now creates one document/scale layout with offsets, page bottoms, height, and maximum width, then uses binary searches for the existing inclusive visible boundaries and strict current/zoom-anchor boundary. It retains the 24-pixel gaps, page positions, target scroll, render DTO, and delayed render cancellation behavior. Mixed-size reference checks cover first/middle/last pages at 1,500 and 65,536 pages, exact equality plus epsilon at both visible boundaries, strict current-page transitions, and scale anchoring. A proxy-backed 65,536-page lookup reads fewer than 80 indexed values; this is an operation-count check, not a frame-rate or memory claim. Mocked Viewer requests cover a far render range and document/revision ownership. The full frontend suite passed 191 tests across 43 files and the production build transformed 1,936 modules in 2.79 seconds. A local browser harness with synthetic 1,500-page data scrolled to page 750, mounted pages 749–750, made three mock render calls, and reported no browser errors. It does not prove native rendering or large-viewer-height behavior. |
| A large Pages sidebar could mount every page button, lose a focused row during virtualization, or treat labels as navigation identifiers. | The sidebar renders a fixed-height virtual track with `ceil(clientHeight / 96) + 12` nearby rows and at most two pinned current/focused rows. Click, Enter, and Space use the existing physical-page action; labels remain secondary DOM text, button names, and titles. Up/Down/Home/End move roving focus through unmounted pages, while Page Up/Page Down stay workspace shortcuts. External page changes recenter without stealing focus, and the document/revision/page-count key prevents scroll or focus from crossing tabs. Scroll updates only the local window and does not request a render or label snapshot. Focused tests cover first/middle/final 65,536-page windows, a tall partial-row viewport, label preservation, keyboard boundaries, count shrink, external current-page changes, tab reset, and no-IPC scroll. The full frontend suite passed 184 tests across 42 files and the production build transformed 1,936 modules. The final standalone debug/no-bundle gate built 1,936 frontend modules in 2.77 seconds and native code in 9.94 seconds; its 30,177,792-byte executable SHA-256 is `1D2BB90033B69E8498F6D2AC415DC2022E265685B0E1153AD5C2FC29C6A5117B`, with exact current JavaScript, CSS, and transformed HTML assets verified embedded. A local browser harness uses synthetic data and no native calls; it showed 17 mounted rows initially, then End/Enter selected physical page 65,536 with 9 final rows mounted. It does not prove end-to-end 65,536-page viewer navigation. |
| A page-image export could render the wrong edited page, accept a stale native result, produce an oversized raster, or mutate the source workspace. | PNG export captures active ID/revision with the current zero-based physical page and sends only 72, 150, or 300 DPI. The UI derives `ceil(points × dpi / 72)` dimensions, checks the 16,384-edge, 32,000,000-pixel, and 128 MiB copied-BGRA-bitmap-buffer limits, and accepts a result only when its nonempty path, identity, DPI, and dimensions exactly match. Native revalidates after the Windows picker, applies the 256 MiB encoded-output limit, and refuses encrypted, restricted, signed, certified, stale, malformed, parser-count/page-tree-disagreed, or unsupported PDFs. The copied-bitmap guard is not a bound on all temporary allocations or total RSS. Current crop, rotation, supported annotations, and supported form appearances flatten to white 8-bit RGB with rounded `pHYs` density metadata; OCR, vectors, editable text, and additional pages are absent. Five focused native tests, the 163-passed/0-failed/1-ignored full native gate in 448.72 seconds, 176 frontend tests across 41 files, mocked-browser keyboard/request/success/cancel/stale/preservation coverage, and independent 2×1-alpha plus edited-792×306/72-DPI-`pHYs`-2,835 checks are recorded. The standalone debug/no-bundle build exited 0 with 1,934 frontend modules (2.74 seconds frontend, 20.15 seconds native); its 30,172,672-byte executable SHA-256 is `D1F1AF4439A2B0586D331761AECDE06B605B4278698E0EC117692DC47F1660EA`, and fresh `index-CwP8sZXB.js` Brotli bytes were verified embedded with the `export_page_image` marker. The browser evidence does not prove real native IPC, Windows picker, or installed-app behavior, which remain open. |
| A malformed middle page ended PDFium iteration silently, exposing only the preceding pages. | Opening now loads every declared page by index and rejects any failure. A fixture with page 2 replaced by a null object is rejected through both opening paths. |
| Different source page counts from PDFium and lopdf could turn Save a Copy into unintended page removal. | Edit sessions retain the original page count. Editing and export reject parser count disagreements; deliberate deletion remains supported. |
| Empty opening passwords let lopdf decrypt automatically, bypassing the old encrypted-file guard. | The editor checks both current encryption and retained decryption state. Actual encrypted fixtures with empty and nonempty passwords reject editing and export and retain identical source bytes. |
| Confirmation-dialog keystrokes could trigger background open/save/undo. | Global shortcuts ignore dialog targets. All three regression tests failed before the guard and pass afterward, while workspace shortcuts still work. |
| The viewer could accept page counts beyond the IPC's 16-bit page index. | Opening rejects documents over 65,536 pages. The limit is a compatibility guard; a huge-document performance claim has not been established. |
| Repeated page-object references could rotate multiple saved pages when only one preview page was edited. | Editing and export reject duplicate leaf references. Regression checks unchanged source, plan, saved baseline, revision and history, plus ordinary-file success. |
| A delayed tab-close response could clear a different active tab or leave an active ID with no document. | Closing holds a synchronous guard and busy state until completion; regression tests cover rapid actions, failures, retry and discard confirmation. |
| PDFium signature-count errors could appear as 65,535 signatures through the wrapper's unsigned cast. | Properties return unknown for the sentinel and never claim signature validity. |
| Page text and Find omitted visible headings on rotated pages and could include text hidden by an offset crop. | Extraction now uses the source-coordinate intersection of the MediaBox and CropBox instead of displayed page dimensions. A regression failed before the fix and passed afterward across all 16 original/edited rotation combinations, independently checking visible header/body words and crop-hidden words in Page text and on-page geometry, unchanged source bytes and rotation, and stale/closed guards. All 45 native tests pass. PDFium reading order can differ with rotation, so the regression does not assume identical full strings. |
| Direct text selection could drift from cropped or rotated rendered pages, accept stale geometry, or expose a partial text layer. | Native returns all-or-nothing normalized glyph geometry for supported cardinal orientations, carries document/page/revision identity, restores temporary page rotation, and caps source text at 20,000 characters. The UI waits for an image, rejects mismatched or malformed replies, measures each browser text range into its native bound, disables selection in pan mode, and routes unsupported/truncated pages to Page text. Native coverage checks crop offsets, all four rotations, edits, stale/closed/out-of-range requests, whitespace and Unicode; focused UI tests cover stale replies, plain text, dimensions, hand mode and fallback. The combined gate passed 66 frontend/release tests, 38 native tests, and the production build. A mocked-IPC Vite harness verified browser selection `A B\né` and matching ranges at 0° and 90°; it is not Windows clipboard or real-native-IPC evidence. |
| A split could publish some outputs, replace an existing folder, use stale page state, or alter the open document. | The native operation revalidates the document revision after the Windows folder dialog, rejects unsupported preservation cases and existing/racing output folders, validates every staged file, then atomically publishes the new folder. It caps output at 64 files and each output at 256 MiB; those are output guards, not an application memory cap. The UI accepts only a positive integer pages-per-file value whose current edited page count produces at most 64 files, blocks duplicate work and closing while it runs, and leaves source, dirty state, save baseline, and undo/redo unchanged. Native tests cover current edited pages across three valid fixtures, rollback, races and preservation guards; focused UI tests cover validation, cancellation, retry, duplicate submission, busy state and unchanged document state. Export, validation, and publication for three outputs took 15.6 ms, 305.2 ms, and 631.6 ms for 5, 97, and 1,499 edited pages respectively; these fixture timings are not whole-application benchmarks. |
| Search results could leave stale page boxes, scan every repeated match per glyph, or imply a location when Page text and geometry use different reading orders. | Search state is tied to document ID/revision and clears on close, query or case changes, tab switches, and revision changes. A shared literal matcher preserves existing `/u` and `/iu` semantics, maps JS code-unit ranges across Unicode and whitespace glyphs, limits the SearchPanel to its first match, and uses a linear cursor for the 20,000-character geometry cap. Pan mode requests geometry for highlights without rendering selectable text; unsupported, capped, malformed, or unmappable matching pages show a route back to Find results or Page text. Focused tests cover literal regex characters, case, repeats, cross-glyph whitespace, astral Unicode, stale state, pan mode, fallback, and page navigation. A mocked-IPC Vite harness measured 0° and 90° rectangles and preserved selected `A B\né`; it does not verify Windows clipboard or real native IPC. The final source gate passed 80 frontend/release tests and the production build. The standalone Tauri debug/no-bundle build passed with 1,932 frontend modules (2.52 seconds frontend, 19.67 seconds native); native desktop interaction remains unverified. |
| Crop could target the wrong page, use source rather than displayed bounds, leave a one-point strip unusable, or mutate a session after stale/failing work. | Organize Pages starts with the current reading page selected and can crop one or more selected physical pages with the same finite, non-negative point insets against each page’s current displayed bounds. It requires a retained width and height of at least one point, previews exact dimensions or mixed-size ranges, blocks duplicate submission and close while busy, and keeps the dialog open on an error. Native conversion accepts only displayed bounds, rechecks document revision and page state, validates every target before one atomic undoable commit, and rejects a batch if any target would be a precision no-op; single-page crop compatibility remains unchanged. Native coverage checks valid fixtures, original and edited rotations, repeated/current crops, one-point boundaries, protected/stale failures, source preservation, and saved/reopened rendered-pixel preservation. Focused UI tests cover mixed sizes, physical selection independent of labels, captured revisions, last-invalid validation, cancellation, retry, duplicate work, and busy state. The mocked-IPC browser harness showed the dimensional preview and raw physical-page request; the full native batch gate passed 149 tests (0 failed, 1 ignored) in 462.16 seconds, and the standalone Tauri debug/no-bundle build passed with 1,932 frontend modules (2.52 seconds frontend, 19.67 seconds native); it is not native desktop evidence. |
| Page-label trees could be malformed, partially traversed, decoded lossily, or displayed for the wrong document revision. | Native traversal bounds references, depth, nodes, ranges, prefixes, generated labels and output size; malformed, partial, encrypted or oversized sources return unavailable with no labels. The reproduced `text_decoding_is_lossless_and_generation_is_bounded` guard covers PDFDocEncoding, undefined bytes, UTF-16BE, UTF-8 BOM and invalid UTF-16. Projection uses the current source order, while the UI requires a complete matching document/revision/page-index snapshot and falls back to physical numbers for stale or malformed replies. The focused native gate passed 9 tests, the full native gate passed 146 tests with 0 failures and 1 ignored test in 501.21 seconds, the frontend suite passed 159 tests, and the production build passed; the mocked browser harness showed duplicate, blank, numeric and whitespace/Unicode labels. The standalone Tauri debug/no-bundle build also passed with 1,932 frontend modules (3.31s frontend, 42.24s native); native desktop interaction remains unverified. |
| Image-to-PDF conversion could accept concatenated, animated, or multi-picture input, lose alpha/color semantics, publish an invalid page, or create output after a canceled or stale operation. | The source build exposes one-image PNG/JPEG creation with Letter/A4, Auto/portrait/landscape, finite 0–72 point margins, contain-and-center placement, upscaling, and a clean output tab. Native validation uses content signatures, scans PNG through its single IEND and JPEG through its EOI while rejecting non-padding trailing data, rejects APNG animation and JPEG MPO markers, applies EXIF orientation, composites alpha onto white, normalizes to 8-bit DeviceRGB/8 bits per component, and rechecks exactly one page and one image XObject before no-clobber publication. Source bytes and open source sessions remain unchanged; ICC profiles and image metadata are intentionally dropped. The explicit blank-margin guard prevents empty input from becoming zero. Focused UI checks cover exact options, validation, cancellation, error retry, busy/duplicate work, source preservation, and clean output activation; the mocked browser harness checks the visible policy and command options. The focused native gate passed 9 tests, including concatenated PNG and MPO refusal and an independent RGBA output probe. The full native gate passed 158 tests (0 failed, 1 ignored) in 726.02 seconds. The standalone Tauri debug/no-bundle build exited 0 with 1,933 frontend modules (5.56 seconds frontend, 35.42 seconds native); its 29,996,032-byte executable SHA-256 is `1C0D41F0E5CB0DA5A2F6FD07EB44985CFBF4B037FD4E401210CFA6B9AF3A55C5`, and embedded `index-89AGNv3S.js` Brotli bytes include the create command marker. Native desktop picker/save-dialog interaction remains unverified. Output filename, existing-target, and output-folder checks now run before source reads and decoding. The focused native gate passed 8 tests with 0 failures; the clean full native gate passed 164 tests with 0 failures and 1 ignored test in 401.94 seconds. The standalone debug/no-bundle build exited 0 with 1,934 frontend modules (3.59 seconds frontend, 13.25 seconds native); its 30,173,696-byte executable SHA-256 is `8BC94E12BCD056CF20FC99A8D29BFD4903491BDD3F23CED02ECAFB81AB0052FF`. Exact current JavaScript, CSS, and HTML asset Brotli bytes were verified embedded. The earlier 176-test frontend suite remains prior evidence; no frontend test rerun accompanied this native path-only update. |
| Default-parallel native tests could contend for the existing four-print-snapshot admission limit. | Test-only print consumers now serialize admission and use RAII cleanup on assertion unwind; a regression verifies capacity is released. Production admission remains four snapshots. The default-parallel native suite passed 52 tests. |
| An unread successful BeginPrint reply could retain an admitted snapshot and exhaust the four-job capacity. | A non-cloneable snapshot lease now queues idempotent cleanup when dropped. The spool thread owns the lease through handoff, source closure, normal completion, and panic; its normal cleanup remains awaited before reporting. The regression failed before the change by rejecting the fifth job, then passed after four unread receivers were dropped. |
| An ordinary Open whose reply closed before delivery could register an unreachable document session. | Open now skips an already closed reply and removes its newly registered document and session when sending the result fails. The regression uses a unique path and a FIFO probe to show no session remains. |
| Successfully sent but unread Open, BeginOpen, Unlock, Combine, or PasswordRequired replies could retain a new session or pending password token. | Private non-cloneable reply leases now clean up only the new document ID or password token until a public service method explicitly accepts its DTO. The five red regressions each retained one item before the fix; afterward, accepted Open, BeginOpen, Unlock, Combine, retry, and cancel results remained usable. An unclaimed Combine still leaves its published PDF intact and reopenable, with source sessions unchanged. This covers worker-to-service receipt only, not acknowledgment by Tauri, WebView, or JavaScript. |
| A FIFO run of live viewer renders could delay Close or EndPrint and retain obsolete render work. | The native request queue looks ahead at most 32 positions only while a viewer Render is at its head. Close and EndPrint can each overtake up to two consecutive renders, while every other request is a strict FIFO barrier and then one render must run. Pure barrier/window/fairness tests and a deterministic actor regression pass. A separate single-fixture manual probe with eight live renders changed Close from 8,985.403 ms with eight renders and 2,931,254 PNG bytes retained to 1.549 ms with zero renders and zero retained bytes; this is not a general performance benchmark. |
| Combining could lose current edits or source history, collide object graphs, merge metadata, publish an invalid/racing output, or falsely succeed after a source changes. | Combine accepts two distinct current document IDs and revisions, checks both before and after the native save dialog, rejects unsupported catalog/page-tree features and unsafe references, renumbers each object graph, and keeps only the first PDF’s Info/XMP metadata. PDFium validates every combined page before tempfile/fsync/noclobber publication. It caps serialized output at 4,096 pages and 256 MiB; those are output guards, not an application memory cap. Native coverage checks all six ordered corpus pairs and every output page, cropped original/edited rotations, pixels/tokens, metadata, stale/closed/duplicate cases, unsupported structures, validation failure, races, source bytes, and source undo/history. UI tests cover order, revisions, cancellation, retry, busy/Escape guards, and activating a clean new tab without changing dirty sources. A mocked-IPC browser harness verified ordered page-count preview, first-metadata notice, and submitted revisions; it is not native save-dialog evidence. |
| Inserting pages could reorder the target, lose its metadata, use stale source plans, or change either source session. | Insert Pages accepts distinct target and donor IDs and current revisions, validates an integer boundary from 0 through the target page count before and after the save dialog, and creates target-prefix + donor + target-suffix output. It retains target Info/XMP metadata even at boundary 0, rejects unsupported structure and existing/racing output paths, validates every output page, and keeps both source histories and dirty state intact. Native tests cover every ordered pair of the three fixtures, all output pages, beginning/middle/end boundaries, cropped original/edited rotations, pixels/tokens, stale/closed/duplicate cases, unclaimed reply cleanup, and source preservation. UI tests cover validation, cancellation/retry, busy/Escape guards, revisions, and activation of a clean five-page copy. A mocked-IPC browser harness checked a 3+2-page boundary preview and its submitted DTO; it is not native save-dialog evidence. |
| Replacing pages could remove the wrong target span, use stale plans, discard target metadata, or let a smaller result bypass structural input guards. | Replace Pages accepts distinct target and donor IDs and current revisions, validates one positive contiguous target range, and creates target-prefix + donor + target-suffix output. It retains target Info/XMP metadata even when the selected span is the whole target, checks the aggregate input page cap before replacement, validates both complete source graphs and every output page, and keeps both source histories and dirty state intact. The source gate passed 109 frontend/release tests, 85 native tests with one separately ignored manual probe, the production frontend build, and a standalone Windows debug build. UI tests cover range and input-cap validation, cancellation/retry, busy/Escape guards, revisions, source preservation, and activation of a clean five-page copy. A mocked-IPC browser harness checked a derived result count and submitted DTO; it is not native save-dialog evidence. A prior interrupted native run had no test-result footer; its cause remains unconfirmed and the captured rerun passed. |
| Sticky notes could drift after crops or rotations, rewrite foreign annotations, leak through unsupported operations, or use stale/busy requests. | Notes use a strict app-owned PDF annotation schema with deterministic icon appearances and stable IDs. The native layer stores source-coordinate bounds, reports clipped displayed bounds or an explicit hidden state, and rejects foreign, empty, malformed, signed, encrypted, form, tagged, page-label, and article-thread annotation structures without mutation. Notes participate in page-plan history, undo/redo, Save a Copy, reopen, supported move/delete/extract/split operations, rendering, and immutable print snapshots; printed pages render the icon, not the note body. Combine, Insert Pages, and Replace Pages refuse annotated sources. The UI keeps its overlay transparent and keyboard-focusable, blocks duplicate/stale/busy operations, does not retarget a stale response after tab or revision changes, and leaves hidden notes editable from the list. Preclosed native mutation receivers are skipped; this does not establish rollback after an already committed mutation whose later JavaScript reply is ignored. |
| Area highlights could map a drag to the wrong cropped or rotated location, double-render a fill, block Pan or Select, retain a stale draft, or rewrite an unsupported annotation. | Area highlights use the same strict owned-annotation and source-coordinate model as notes. Native validation requires one displayed rectangle of at least one PDF point, a canonical Highlight schema with QuadPoints and fixed translucent-yellow appearance, and refuses foreign or protected annotation structures. The UI measures the rendered image client rectangle, clamps and cancels drafts on pointer cancel, lost capture, page movement, zoom/layout change, or revision change. It keeps the first pointer identity and ignores additional or mismatched pointers. Highlight mode disables existing hit targets for overlapping drags; Pan and Select keep them inactive, while Comment mode alone exposes transparent keyboard-focusable targets. Highlights have optional bodies normalized by native code, share the 1,000-annotation, 1 MiB total-text, and 8 KiB-per-body limits, and retain their geometry through supported operations, undo/redo, Save a Copy, reopen, native rendering, and immutable print snapshots. The final gate passed 132 frontend/release tests and 99 native tests with one ignored manual probe, plus production frontend and standalone Windows debug builds. Focused UI tests and a mocked browser drag harness verify request geometry, cancellation, mode behavior, transparent targets, and Unicode body handling; this is not native desktop interaction, saved-output, print, or installed-app evidence. Area highlights are rectangles, not text-selection markup. |
| A selected-text highlight could use a DOM rectangle or a stale/cross-page range, split a Unicode glyph, let one text layer clear another layer's action, or create too many hit targets. | The UI accepts one current mounted text-layer range only, maps browser UTF-16 endpoints outward to whole `page_text_geometry.characters` entries, preserves the newest selection owner, and invalidates an older toolbar pointer capture when a new valid selection arrives. Regressions reject a geometry-index node from another layer, prevent an older layer's clear or unmount from clearing the newer owner, and discard a canceled toolbar pointer capture after a newer selection. Native recomputes the same current geometry and paints at most 256 positioned glyph quads, including multiline ranges; null-bound whitespace stays logical but unpainted. Native keeps a 4,096-slot document geometry budget. The UI renders no page targets when actual visible targets exceed 512 and routes editing to the complete Comments list. The full source gate passed 144 frontend/release tests and 108 native tests with one ignored manual probe, the production frontend build, and the standalone Windows debug build. Focused UI tests cover multiline and keyboard selection, whole-glyph Unicode mapping, cross-layer/stale disposal, pointer capture, and the 512/513 boundary; a mocked browser harness uses real DOM ranges, but does not prove native desktop save, rendering, print, or installed-app behavior. The pinned engine's emoji (astral-character) source-mapping fixture is explicitly unsupported, so no source mapping is guessed. |
| Filling a form could rewrite an unsupported field, lose its existing appearance, select or clear the wrong button state, publish a partial output, or alter the open source session. | Fill forms parses only a strict flat single-line `/Tx` subset, flat merged checkbox widgets, one-level same-page Radio plus NoToggleToOff groups with two through 32 options, and flat merged single-select choice fields with one through 32 options. Text values are printable ASCII with a fixed supported Helvetica appearance; each checkbox requires Off plus one opaque on state in normal and any rollover or down appearances. A supported radio group uses exact validated source option states and labels, may be blank only when its parent V is absent or Off and every child AS is Off, and cannot be cleared after a choice. A supported choice is a noneditable dropdown or a fully visible list box: its exact display labels are exposed, while its private export values remain native-only. Its blank `/V` and `/I` state must agree; a patch selects one source option and cannot clear it. The parser rejects editable, multi-select, scrolled, ambiguous, or non-fitting choices along with actions, XFA, signatures, encryption, hidden or read-only flags, non-flat/ambiguous or cross-page groups, shared/missing widgets, duplicate/unknown states, custom printable-ASCII encodings, unknown appearance content, and changed page plans before mutation. Text and semantically changed choice fields regenerate a normal appearance; checkbox changes retain original appearance/default/style data and switch only canonical V and AS states; radio changes preserve raw appearances/defaults and switch the parent V and child AS states only. Every requested tagged patch validates before a document write, and submitted fields with no semantic change retain their raw original objects. The result is reparsed, opened, and rendered before no-clobber publication, then opens as a clean new session while retaining the source tab. Engine checks compare validated opaque checkbox states, exact radio group values, and choice label/selection order rather than assuming a Yes state or trusting display text as an export value. A ReportLab compatibility fixture checks changed field pixels, values, untouched source bytes, reopen, crop/rotation preservation, and immutable print snapshot bitmaps; three corpus fixtures check every original page with an appended independent form page. Independent pypdf field-tree/widget/default/appearance inspection and Poppler rendering support supplied compatibility cases, not broad Acrobat-form compatibility. The choice UI update passed the 153-test frontend suite and production frontend build, its focused native gate passed 8 tests, its full native gate passed 137 tests with one ignored manual probe in 346.99 seconds, and its standalone Tauri Windows debug/no-bundle build passed. The UI sends explicit tagged changed-only patches, keeps drafts after cancellation or failure, and blocks stale, duplicate, Escape, close, and busy interactions. Its local-browser harness uses mocked IPC, so native save-dialog, installed-app, and wider cross-viewer evidence remain separate. |
| The full frontend suite could time out the existing 500-page SearchPanel cap and offline Cargo metadata/archive checks under worker contention. | The two assertions passed alone and together; the earlier full-run times of 5.221 s and 11.658 s exceeded Vitest's default 5-second budget. Only these two high-work tests receive a 15-second local budget. The 500-page cap, navigation assertions, offline locked metadata, and archive/hash/source validation are unchanged. This supports a scheduling-dependent deadline inference, not a product performance result or a CPU-cause conclusion. |

Password tests also cover wrong/correct passwords, canceled or reused request tokens, rendering after unlock, delayed successful results after cancellation, cleared input, and simulated React effect replay. Native encryption fixtures cover RC4-128 V2, AES-128 V4 and AES-256 V5 with protected and empty passwords, rendering every fixture page. Other encryption variants and Windows password-dialog interaction remain unverified.

Updater tests cover interrupted downloads, timeouts, retry, reset progress counters, busy-state restoration, blocked Close/Escape while installing, duplicate-click suppression, and resource cleanup. These use mocked updater calls and do not establish Windows installer recovery.

## Open risks and verification work

- A brief forms audit raised hardening hypotheses. Its small fixture did not reproduce an omission, the review tool blocked follow-up, and no forms change was validated or retained; this remains unverified work.
- File reads and PDFium's source copy have no total memory limit, and the worker queue remains unbounded. Undo/redo snapshot allocations now share a 32 MiB per-document budget with oldest-history eviction. Close and EndPrint can bypass a bounded head run of viewer renders, but broader rapid-scroll and memory-pressure limits still need measurement.
- Matching page counts do not prove that two parsers repair a damaged page tree in the same order. More damaged-document fixtures are needed.
- Native parser crash isolation, fuzzing, a real-document compatibility corpus and sustained memory-pressure tests remain missing.
- Reply leases stop when a public service method accepts and returns a plain DTO. They do not establish that Tauri, WebView, or JavaScript received that result; post-acceptance transport loss remains unverified.
- Installed-app password/clipboard/restart behavior, including on-page selection with real native geometry, crop interaction, the split folder dialog, and the Combine Files, Insert Pages, Replace Pages, and annotation save dialogs, native print output and driver failures, the signed installer upgrade from 0.2.0, interrupted-install recovery and relaunch still require end-to-end verification.
- The [dependency inventory](license-inventory.md) records current license evidence. General notices and five exact MPL source archives are generated, bundled, and hash-checked in fresh unsigned-debug and signed v0.2.6-draft extractions. The signed draft’s 26 packaged resources, manifest, updater assets, and publisher signatures were independently checked. Project licensing, WebView2 distribution terms, and installed notice interaction remain unresolved.
- The historical [v0.2.5 workflow](https://github.com/Joshua-Beel/Smacrobat/actions/runs/35250005735) stopped during npm tests because the clean runner had not fetched the locked Cargo registry needed by offline dependency-notice checks. No native test, signing, installer creation, upload, or release step ran, so it supplies no signed-artifact or installed-app evidence. The 0.2.6 candidate adds that locked Windows fetch before the unchanged offline checks.
- The [v0.2.6 workflow](https://github.com/Joshua-Beel/Smacrobat/actions/runs/35250878119) completed before the privacy rewrite and produced a signed, unpublished draft. The rewritten [`v0.2.6` tag](https://github.com/Joshua-Beel/Smacrobat/commit/8cf90166623857dddd3854ce769549da55f6f5c4) records the cleaned source history; the draft assets were not rebuilt or re-signed. Independent artifact checks verified all three assets, manifest URLs and signatures with negative controls, timestamped publisher signatures, 26 packaged resources, five MPL archives, and bundled notices. Installer SHA-256: `ec47981b533fdf4eb00d9fe62e52edd7bc1ef160cda9c18d962f0e1f39dfe255`. It does not establish installation, upgrade, relaunch, or installed-app behavior.

The replacement source gate passed 109 frontend/release tests, 85 native tests with one separately ignored manual probe, the production frontend build, and a standalone Windows debug build. A prior interrupted native run ended after 179 seconds without a test failure or result footer; its cause remains unconfirmed, and the captured rerun passed. The prior source gate passed 102 frontend/release tests, 79 native tests, the production frontend build, and a standalone Windows debug build. The scheduler’s separate manual probe had already passed and was not part of that native test count. The fresh unsigned debug installer extraction hash-checked all five MPL source archives, their manifest, and both notice files. The same resources were independently verified in the signed v0.2.6 draft, which includes source work through Replace Pages; later annotation work is source-only. These checks do not establish installed-app behavior.
