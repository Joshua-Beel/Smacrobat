# Remaining development and verification

The README records what is implemented and tested. This plan organizes the remaining work; a completed source feature does not establish that the installed application works. The original brief and `gaps.md` remain the broader feature reference.

## Team rules

- GPT-6 handles planning, ownership, dependencies and acceptance decisions.
- GPT-5.6 Sol handles complex native PDF work and adversarial review; GPT-5.6 Terra handles UI and routine integration; GPT-5.6 Luna handles bounded exploration when a slot is available.
- Each implementation has explicit file ownership and a defined interface. Agents coordinate shared contracts before editing consumers.
- Only one native test/build process runs at a time. Combined tests and applicable artifact checks must pass before explicit-path commits and pushes.
- Each feature receives an independent review where capacity permits. Findings need evidence and regression coverage; unsupported or unverified behavior stays documented.

## Work sequence

| Slice | Dependencies | Acceptance evidence |
| --- | --- | --- |
| Page labels and page boxes | Read-only display now projects supported source labels onto the current page order; page boxes and label editing, jumps, and export ranges remain future work | Exact ranges and boxes survive reopening; future label editing and label-based navigation/export preserve source state; rotated and mixed-size documents are covered. |
| Batch crop | Reuse Organizer selection and current displayed page bounds | Apply equal finite point insets to selected physical pages, retain at least 1 point in both dimensions on every target, validate all targets before one undoable commit, and show dimensional-only ranges for mixed sizes or rotations. The full native batch gate passed 149 tests (0 failed, 1 ignored) in 462.16 seconds, and the standalone Tauri debug/no-bundle build passed with 1,932 frontend modules (2.52 seconds frontend, 19.67 seconds native); native desktop interaction remains open. |
| Create a PDF from an image | One PNG or JPEG source and one clean output tab | The source build supports Letter/A4, Auto/portrait/landscape, finite 0–72 point margins, contain-and-center placement, EXIF orientation, white transparency compositing, and 8-bit RGB output. Animation, multi-picture data, malformed/trailing input, and bounded oversize images are refused; ICC profiles and image metadata are not retained. The full native gate passed 158 tests (0 failed, 1 ignored) in 726.02 seconds; the standalone Tauri debug/no-bundle build exited 0 with 1,933 frontend modules (5.56 seconds frontend, 35.42 seconds native). Native desktop picker/save-dialog interaction remains open. |
| Native verification after the 0.2.6 draft | A current signed draft and desktop-control verification | The pre-rewrite 0.2.5 tag's run stopped during npm tests before native/signing/build work. The signed v0.2.6 draft from run 35250878119 is artifact-verified; desktop save dialogs, installation, update, relaunch, and publisher behavior still need direct evidence. |
| Installed printing and update verification | A current signed installer; desktop-control verification remains blocked | Worker-reply cleanup covers raw worker-to-service receivers but not frontend acknowledgment. Inspect Print to PDF output and printer cancellation; install and upgrade from 0.2.0, preserve documents/settings, relaunch and exercise interruption/retry. Check the installed notice entry and access to packaged source resources. |
| Comments and highlights | Final native gate and native desktop verification | The current source work creates, selects, edits, and deletes strict app-owned sticky notes, fixed area highlights, and supported selected-text highlights. Reopen, undo/redo, supported page operations, crops/rotations, and immutable print snapshots need to retain their position/content; foreign or protected annotations must remain explicitly unavailable. Replies, author identity, and annotation interchange remain separate work. |
| Selected-text highlights | Stable text geometry and annotation persistence | A supported browser selection maps its UTF-16 endpoints outward to whole half-open `page_text_geometry.characters` array indices, then native code derives the saved quads. It needs multiline/Unicode bounds, stale revisions, caps, saved appearance, reopen, undo, and print evidence. It is source-only work after the 0.2.6 draft, not an Acrobat-parity claim. |
| Fill existing forms, then form preparation | Form-aware preservation and save validation | The current source slice fills a strict subset of flat single-line text fields, flat merged checkboxes, standard one-level same-page radio groups, and single-select dropdowns or fully visible list boxes into a new copy. It still needs cross-viewer, native desktop save-dialog, and installed-app evidence; editable combos, multi-select or scrolling lists, unsupported radio groups, signatures, and general form preparation remain separate work. |
| Text and image editing | Font/image handling and content-preserving export | Changed content, appearance and undo survive reopening; unsupported fonts/layouts cannot silently corrupt or substitute content. |
| OCR and conversion | Engine choice, dependency/license review and document model | Scans produce aligned searchable text; representative layouts and languages have measured accuracy; conversion losses are disclosed and originals preserved. |
| Redaction and sanitization | Content editing and extraction-aware verification | Removed content cannot be recovered through text, objects, images, attachments or metadata; reopen/render and adversarial extraction checks prove removal. Visual masking alone never qualifies. |
| Resource limits, isolation and recovery | Worker lifecycle and persistence design | Measure large-file and rapid-scroll workloads; bound queued/retained work; recover from worker failure without losing acknowledged edits; test corrupt recovery data. |
| Compatibility, accessibility and remaining tools | Incremental feature implementations | Expand beyond synthetic fixtures to a documented real-file corpus; test keyboard/focus behavior and cross-viewer output; turn remaining `gaps.md` entries into bounded slices. |
| Release readiness and final adversarial review | Applicable preceding slices | Verify packaged assets and installed behavior, resolve remaining license/source-availability items, review malformed inputs and failure paths, and publish only evidence-backed release notes. |

## Current assignments

On-page selection, fixed-size splitting, search highlighting, current-page crop, Combine Files, Insert Pages, and Replace Pages are accepted for the source build. Selection has combined-gate evidence for geometry, stale-response handling, caps and browser DOM selection; installed Windows clipboard behavior with real native IPC remains open. Splitting has combined-gate evidence for current edited page order, all-output validation, atomic folder publication, output caps, bounded UI input, and unchanged source/session state; the native folder dialog remains open for desktop verification. Search highlighting has source-gate evidence for stale search state, literal Unicode range mapping, geometry fallbacks, pan-mode behavior, and mocked browser DOM boxes/selection; real native IPC and Windows clipboard behavior remain open. Crop has combined-gate evidence for current-page initialization, rotated displayed dimensions, finite point insets, one-point retained bounds, stale/error/duplicate handling, source preservation, and rendered output; its browser harness uses mocked IPC and desktop interaction remains open. Combine and Insert Pages have combined-gate evidence for current edits, ordered all-pair fixture output, target/first Info/XMP preservation, output validation, source-session preservation, stale/closed guards, unsupported-structure refusal, output caps, and mocked browser revision/boundary checks; their native save dialogs remain open for desktop verification. Replace Pages has a 109-frontend/release-test and 85-native-test source gate, production frontend build, and standalone Windows debug build for contiguous ranges, current revisions, output count, target metadata, source-session preservation, and mocked browser requests; its native save dialog remains open for desktop verification. A prior interrupted native run had no result footer; its cause remains unconfirmed and the captured rerun passed. Sticky-note comments, fixed area highlights, and supported selected-text highlights are current source work. The selected-text full source gate passed 144 frontend/release tests and 108 native tests with one ignored manual probe, the production frontend build, and the standalone Windows debug build. The text tool maps a one-page browser selection outward to whole geometry-array characters and native code paints its quads; it does not infer text from screen rectangles or promise visual reading order. A text highlight uses at most 256 painted quads, the document has 4,096 annotation geometry slots, and a page with more than 512 visible edit targets routes users to the Comments list without losing native paint or list entries. Unsupported or truncated geometry, including the pinned engine's astral mapping fixture, cannot create a text highlight; whitespace with no bounds remains logical only. Desktop annotation interaction and installed-app behavior remain unverified. Print and document/password reply cleanup are accepted for the source build: all raw worker-reply red cases have regression coverage, but later frontend transport receipt remains unverified. Worker scheduling now bounds cleanup lookahead across viewer renders, and the five exact MPL archive resources are hash-checked in a fresh unsigned debug installer extraction and the signed v0.2.6 draft. The pre-rewrite 0.2.5 workflow stopped during npm tests before native, signing, build, asset, or draft-release work. Run 35250878119 produced the verified signed draft before the privacy rewrite; the rewritten [`v0.2.6` tag](https://github.com/Joshua-Beel/Smacrobat/commit/8cf90166623857dddd3854ce769549da55f6f5c4) records the cleaned source history. The draft was not rebuilt or re-signed, remains unpublished, and desktop installation, update, relaunch, save-dialog, and publisher behavior remain open.

The current source slice can fill a strict supported subset of existing plain text fields, flat merged checkboxes, standard one-level same-page radio groups, and single-select choice fields into a new copy. It preserves the original source session, validates text values and regenerated text appearances, retains validated checkbox appearances while changing their source state, preserves validated radio appearances/defaults while changing a selected group’s current state, and maps opaque choice IDs to source-private export values while showing source display labels. A radio source group or choice field may be blank only under its exact validated source state, but this no-clear subset cannot clear after selection. Choice lists must fit every source option and dropdowns are noneditable. The frontend suite passed 153 tests and the production frontend build passed; the focused native choice gate passed 8 tests and the full native gate passed 137 tests with one ignored manual probe in 346.99 seconds. The standalone Tauri Windows debug/no-bundle build passed with 1,931 frontend modules in 3.10 seconds and native compilation in 19.25 seconds. Form preparation, Fill & Sign, certificates, editable combos, multi-select lists, unsupported radio groups, and general Acrobat form compatibility remain open. Native desktop save-dialog, installed-app, and broader cross-viewer evidence are still required.

Create a PDF from an image is current source work for one PNG or JPEG converted to one clean new PDF tab. The UI covers the exact page policy, picker cancellation, blank-margin validation, busy and duplicate guards, failure retry, source preservation, and output activation; the mocked browser harness covers the visible policy and command options. Native conversion independently covers bounded input, EXIF orientation, alpha compositing, centered contain placement, one-page/one-image output validation, no-clobber publication, and source preservation. The full native gate passed 158 tests (0 failed, 1 ignored) in 726.02 seconds, and the standalone Tauri debug/no-bundle build exited 0 with 1,933 frontend modules (5.56 seconds frontend, 35.42 seconds native). Native picker/save-dialog interaction remains open.

The signed 0.2.4 draft predates current source work. The signed but unpublished 0.2.6 draft includes source work through Replace Pages; current comments, highlights, form filling, and image creation were added afterward. Neither draft is evidence of installed-app behavior. No full Acrobat-parity or final-review completion claim is warranted while the remaining gaps are open.
